import { SovereignHUD } from '@/components/hud/SovereignHUD';

export default function DashboardPage() {
  // The SovereignHUD acts as a full-bleed application shell, completely replacing the legacy page layout.
  return <SovereignHUD />;
}
