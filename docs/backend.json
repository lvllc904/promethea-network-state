{
  "entities": {
    "Citizen": {
      "title": "Citizen",
      "type": "object",
      "description": "Represents a citizen within the Promethea Network State.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Citizen entity."
        },
        "decentralizedId": {
          "type": "string",
          "description": "The citizen's Decentralized Identifier (DID) - their Promethean Passport."
        },
        "reputationScore": {
          "type": "number",
          "description": "A non-transferable score representing the citizen's proven history of positive contributions."
        },
        "contributionScore": {
          "type": "number",
          "description": "A score representing the citizen's overall contributions to the network (sweat equity + capital invested)."
        },
        "personhoodScore": {
          "type": "number",
          "description": "A score calculated using Quadratic Voting principles to prevent domination by large token-holders."
        },
        "skills": {
          "type": "array",
          "description": "An array of verifiable skills possessed by the citizen (Verifiable Credentials).",
          "items": {
            "type": "string"
          }
        }
      },
      "required": [
        "id",
        "decentralizedId",
        "reputationScore",
        "contributionScore",
        "personhoodScore"
      ]
    },
    "Proposal": {
      "title": "Proposal",
      "type": "object",
      "description": "Represents a governance proposal within the Promethean DAC.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Proposal entity."
        },
        "proposerId": {
          "type": "string",
          "description": "Reference to Citizen who proposed the proposal. (Relationship: Citizen 1:N Proposal)"
        },
        "title": {
          "type": "string",
          "description": "The title of the proposal."
        },
        "description": {
          "type": "string",
          "description": "A detailed description of the proposal."
        },
        "status": {
          "type": "string",
          "description": "The current status of the proposal (e.g., 'Draft', 'Active', 'Passed', 'Rejected')."
        },
        "votingStartTime": {
          "type": "string",
          "description": "The date and time when voting on the proposal starts.",
          "format": "date-time"
        },
        "votingEndTime": {
          "type": "string",
          "description": "The date and time when voting on the proposal ends.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "proposerId",
        "title",
        "description",
        "status"
      ]
    },
    "RealWorldAsset": {
      "title": "RealWorldAsset",
      "type": "object",
      "description": "Represents a Real-World Asset (RWA) within the Promethean Network State.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the RealWorldAsset entity."
        },
        "name": {
          "type": "string",
          "description": "The name of the real-world asset (e.g., 'Apartment Complex A', 'Farmland B')."
        },
        "description": {
          "type": "string",
          "description": "A detailed description of the real-world asset."
        },
        "location": {
          "type": "string",
          "description": "The geographical location of the real-world asset."
        },
        "assetType": {
          "type": "string",
          "description": "The type of asset (e.g., 'Real Estate', 'Agriculture', 'Small Business')."
        },
        "tokenIds": {
          "type": "array",
          "description": "References to UVT tokens representing fractional ownership of the asset. (Relationship: RealWorldAsset 1:N UniversalValueToken)",
          "items": {
            "type": "string"
          }
        }
      },
      "required": [
        "id",
        "name",
        "description",
        "location",
        "assetType"
      ]
    },
    "UniversalValueToken": {
      "title": "UniversalValueToken",
      "type": "object",
      "description": "Represents a Universal Value Token (UVT) within the Promethean Network State.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the UniversalValueToken entity."
        },
        "ownerId": {
          "type": "string",
          "description": "Reference to Citizen who owns the token. (Relationship: Citizen 1:N UniversalValueToken)"
        },
        "assetId": {
          "type": "string",
          "description": "Reference to RealWorldAsset the token represents fractional ownership of. (Relationship: RealWorldAsset 1:N UniversalValueToken)"
        },
        "tokenType": {
          "type": "string",
          "description": "The type of token (e.g., 'Labor', 'Capital', 'Reputation')."
        },
        "amount": {
          "type": "number",
          "description": "The amount of the token held."
        }
      },
      "required": [
        "id",
        "ownerId",
        "assetId",
        "tokenType",
        "amount"
      ]
    },
    "Vote": {
      "title": "Vote",
      "type": "object",
      "description": "Represents a vote cast by a citizen on a proposal.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Vote entity."
        },
        "citizenId": {
          "type": "string",
          "description": "Reference to Citizen who cast the vote. (Relationship: Citizen 1:N Vote)"
        },
        "proposalId": {
          "type": "string",
          "description": "Reference to Proposal being voted on. (Relationship: Proposal 1:N Vote)"
        },
        "votePower": {
          "type": "number",
          "description": "The voting power used by the citizen in this vote."
        },
        "support": {
          "type": "boolean",
          "description": "Indicates whether the citizen supported the proposal (true) or opposed it (false)."
        }
      },
      "required": [
        "id",
        "citizenId",
        "proposalId",
        "votePower",
        "support"
      ]
    },
    "Task": {
      "title": "Task",
      "type": "object",
      "description": "Represents a task related to real-world asset management.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Task entity."
        },
        "assetId": {
          "type": "string",
          "description": "Reference to RealWorldAsset the task is associated with. (Relationship: RealWorldAsset 1:N Task)"
        },
        "assigneeId": {
          "type": "string",
          "description": "Reference to Citizen assigned to the task. (Relationship: Citizen 1:N Task)"
        },
        "description": {
          "type": "string",
          "description": "A detailed description of the task."
        },
        "status": {
          "type": "string",
          "description": "The current status of the task (e.g., 'Open', 'In Progress', 'Completed')."
        },
        "priority": {
          "type": "string",
          "description": "The priority of the task (e.g., 'High', 'Medium', 'Low')."
        },
        "dueDate": {
          "type": "string",
          "description": "The due date for the task.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "assetId",
        "description",
        "status",
        "priority",
        "dueDate"
      ]
    },
    "DACParams": {
      "title": "DACParams",
      "type": "object",
      "description": "Represents parameters within the Promethean DAC.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the DACParam entity."
        },
        "name": {
          "type": "string",
          "description": "Name of the parameter (e.g., initialReputation, governanceThreshold)."
        },
        "value": {
          "type": "string",
          "description": "Value of the parameter (can be string, number, boolean, etc.)."
        },
        "description": {
          "type": "string",
          "description": "Description of the parameter."
        }
      },
      "required": [
        "id",
        "name",
        "value",
        "description"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "/citizens/{citizenId}": {
      "schema": {
        "$ref": "#/entities/Citizen"
      },
      "description": "Stores citizen data. Authorization is based on path-based ownership and potentially denormalized role data within the document itself."
    },
    "/proposals/{proposalId}": {
      "schema": {
        "$ref": "#/entities/Proposal"
      },
      "description": "Stores governance proposals. Access control is managed by the proposerId and potentially a membership map for collaborative proposals."
    },
    "/real_world_assets/{assetId}": {
      "schema": {
        "$ref": "#/entities/RealWorldAsset"
      },
      "description": "Stores real-world asset data. Authorization might involve a membership map indicating authorized users for managing the asset, denormalized for authorization independence."
    },
    "/universal_value_tokens/{tokenId}": {
      "schema": {
        "$ref": "#/entities/UniversalValueToken"
      },
      "description": "Stores universal value tokens. Ownership is determined by the ownerId field, which references a citizen. Includes assetId for linking to RealWorldAsset."
    },
    "/votes/{voteId}": {
      "schema": {
        "$ref": "#/entities/Vote"
      },
      "description": "Stores votes cast by citizens. Access is controlled by the citizenId and proposalId."
    },
    "/tasks/{taskId}": {
      "schema": {
        "$ref": "#/entities/Task"
      },
      "description": "Stores tasks related to real-world asset management. Authorization is based on the assigneeId and the assetId."
    },
    "/dac_params/{dacParamsId}": {
      "schema": {
        "$ref": "#/entities/DACParams"
      },
      "description": "Stores parameters for the DAC, such as governance thresholds or initial reputation values. Access should be restricted to administrators or potentially the DAC itself via a service account."
    }
  }
}